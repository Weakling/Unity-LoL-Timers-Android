using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// currently does nothing

public class ChampionPanel : MonoBehaviour {

    /*
    MyManager myManager;

    private void Awake()
    {
        myManager = FindObjectOfType<MyManager>();
    }

    public void Up()
    {
        if(myManager.championPanelCtr > 0)
        {
            myManager.championPanelCtr--;
            myManager.championPanelsArray[myManager.championPanelCtr].SetActive(true);
            this.transform.gameObject.SetActive(false);
        }
    }

    public void Down()
    {
        if (myManager.championPanelCtr < myManager.championPanelsArray.Length - 1)
        {
            myManager.championPanelCtr++;
            myManager.championPanelsArray[myManager.championPanelCtr].SetActive(true);
            this.transform.gameObject.SetActive(false);
        }
    }*/
}